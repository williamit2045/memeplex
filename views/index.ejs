
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Memeplex</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <nav class="bg-white shadow-lg mb-8">
        <div class="max-w-6xl mx-auto px-4">
            <div class="flex justify-between">
                <div class="flex space-x-7">
                    <a href="/" class="flex items-center py-4">
                        <span class="font-semibold text-gray-500 text-lg">Memeplex</span>
                    </a>
                    <a href="/create" class="flex items-center py-4 text-gray-500 hover:text-gray-700">
                        Create Content
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto">
            <h1 class="text-4xl font-bold text-gray-900 mb-8">
                Welcome to Memeplex
            </h1>
            
            <div class="bg-white shadow-lg rounded-lg p-6 mb-8">
                <p class="text-gray-600 mb-4">
                    Start creating and organizing content with Memeplex.
                </p>
                <a href="/create" 
                   class="inline-block bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">
                    Create New Content
                </a>
            </div>
    
            <div class="space-y-6">
                <% if (typeof content !== 'undefined' && content.length > 0) { %>
                    <% content.forEach(item => { %>
                        <div class="bg-white shadow-lg rounded-lg p-6">
                            <h2 class="text-2xl font-bold mb-2">
                                <a href="/view/<%= item.id %>" class="text-blue-600 hover:text-blue-800">
                                    <%= item.title %>
                                </a>
                            </h2>
                            <p class="text-gray-600">
                                <%= item.body.substring(0, 200) %>...
                            </p>
                            <div class="mt-4 text-sm text-gray-500">
                                Posted <%= new Date(item.created_at).toLocaleDateString() %>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-gray-600">No content yet. Be the first to create something!</p>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>